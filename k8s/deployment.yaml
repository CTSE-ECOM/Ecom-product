apiVersion: apps/v1
kind: Deployment
metadata:
  name: ctse-deployment
  labels:
    app: ctse-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: ctseapp
  template:
    metadata:
      labels:
        app: ctseapp
    spec:
      containers:
        - name: ctse-app
          image: deshi97/ctse-assignment:v3
          imagePullPolicy: Always
          ports:
            - containerPort: 6500
