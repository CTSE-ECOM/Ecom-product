apiVersion: apps/v1
kind: Deployment
metadata:
  name: ctse-deployment
  labels:
    app: ctse-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: ctse
  template:
    metadata:
      labels:
        app: ctse-app
    spec:
      containers:
        - name: ctse-app
          image: deshi97/ctse-assignment:v3
          imagePullPolicy: Always
          ports:
            - containerPort: 6500
